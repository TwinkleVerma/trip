<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account', 'https://'+Rails.application.config.action_mailer.default_url_options[:host].to_s+':'+Rails.application.config.action_mailer.default_url_options[:port].to_s+confirmation_path(@resource, confirmation_token: @token).to_s %></p>
